import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  overrides: {

  },

  palette: {
    secondary: {
      light: '#9E9D9D',
      main: '#333333',
      dark: '#000000',
      contrastText: '#fff',
    }
  }
});

export default theme;
